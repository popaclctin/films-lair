import styled from 'styled-components';

export const Wrapper = styled.tr`
  .actor__profile {
    div {
      height: 3rem;
    }

    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }
`;
