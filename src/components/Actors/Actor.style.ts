import styled from 'styled-components';

export const Wrapper = styled.tr`
  .actor__profile {
    /* div {
      height: 4rem;
    } */

    img {
      width: 100%;
      height: auto;
      object-fit: cover;
    }
  }
`;
