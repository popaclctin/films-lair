import styled from 'styled-components';

export const Wrapper = styled.ul`
  padding: 0 1rem;
  width: min(25rem, 80%);
  text-align: center;

  display: grid;
  place-items: center;
  row-gap: 2rem;
`;
